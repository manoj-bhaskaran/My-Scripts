# SonarCloud Project Configuration
# https://docs.sonarcloud.io/advanced-setup/analysis-parameters/

sonar.projectKey=manoj-bhaskaran_My-Scripts
sonar.organization=manoj-bhaskaran

# Project metadata
sonar.projectName=My Scripts Collection
sonar.projectVersion=1.0.0

# Source and test directories
sonar.sources=src
sonar.tests=tests

# File inclusions
sonar.inclusions=**/*.py,**/*.ps1,**/*.psm1,**/*.bat,**/*.sql

# Coverage exclusions
sonar.coverage.exclusions=**/tests/**,**/test_*,**/*.Tests.ps1

# Python coverage
sonar.python.coverage.reportPaths=coverage/python/coverage.xml

# PowerShell coverage (JaCoCo format)
sonar.coverage.jacoco.xmlReportPaths=coverage/powershell/coverage.xml

# Quality Gate - Do not wait for quality gate result
# This allows the build to pass even if quality gate fails
# Temporary setting while establishing test coverage baseline
sonar.qualitygate.wait=false

# Language-specific settings
sonar.python.version=3.10,3.11,3.12
sonar.python.file.suffixes=.py

# PowerShell settings
sonar.powershell.file.suffixes=.ps1,.psm1

# Exclude generated or vendor code
sonar.exclusions=**/node_modules/**,**/vendor/**,**/__pycache__/**,**/.pytest_cache/**

# Additional analysis properties
sonar.sourceEncoding=UTF-8
